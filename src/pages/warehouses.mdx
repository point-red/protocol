export const description =
  'On this page, we’ll dive into the different warehouse endpoints you can use to manage warehouses programmatically.'

# Warehouse

Master Warehouse

## The warehouse model

The warehouse model contains all the information about your warehouses.

### Properties

<Properties>
  <Property name="_id" type="string">
    Unique identifier for the warehouse.
  </Property>
  <Property name="name" type="string">
    The name for the warehouse.
  </Property>
  <Property name="permissions" type="array<string>">
    The array of allowed permission name for the warehouse.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who create warehouse.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who update warehouse.
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the warehouse was created.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the warehouse was updated.
  </Property>
</Properties>

---

## List all warehouses {{ tag: 'GET', label: '/v1/warehouses' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your warehouses. By default, a maximum of ten warehouses are shown per page.

    ### Optional attributes

    <Properties>
      <Property name="page" type="integer">
        The page number of warehouses returned.
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of warehouses returned.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/warehouses">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/warehouses', {
      params: {
        filter: {
          page:1,
          pageSize: 10
        }
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "warehouses": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "name": "Super Admin",
          "permissions": ["create-user", "read-user-warehouse"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
        {
          "_id": "637d83d15d2be122007524bf",
          "name": "Manager",
          "permissions": ["create-user", "read-user-warehouse"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create a warehouse {{ tag: 'POST', label: '/v1/warehouses' }}

<Row>
  <Col>

    This endpoint allows you to add a new warehouse.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the warehouse.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/warehouses">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/warehouses', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve a warehouse {{ tag: 'GET', label: '/v1/warehouses/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve a warehouse by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/warehouses/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/warehouses/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "637d83d15d2be122007524bf",
      "name": "Manager",
      "permissions": ["create-user", "read-user-warehouse"],
      "createdBy": "637d83d15d2be122007524bf",
      "createdAt": "2022-01-01T00:00:00.000Z",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update a warehouse {{ tag: 'PATCH', label: '/v1/warehouses/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a warehouse.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the warehouse.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/warehouses/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/warehouses/637d83d15d2be122007524bf', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete a warehouse {{ tag: 'DELETE', label: '/v1/warehouses/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete warehouses from your warehouse list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/warehouses/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/warehouses/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---
