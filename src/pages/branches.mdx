export const description =
  'On this page, we’ll dive into the different branch endpoints you can use to manage branchs programmatically.'

# Branch

Master Branch

## The branch model

The branch model contains all the information about your branchs.

### Properties

<Properties>
  <Property name="_id" type="string">
    Unique identifier for the branch.
  </Property>
  <Property name="name" type="string">
    The name for the branch.
  </Property>
  <Property name="permissions" type="array<string>">
    The array of allowed permission name for the branch.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who create branch.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who update branch.
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the branch was created.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the branch was updated.
  </Property>
</Properties>

---

## List all branchs {{ tag: 'GET', label: '/v1/branchs' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your branchs. By default, a maximum of ten branchs are shown per page.

    ### Optional attributes

    <Properties>
      <Property name="page" type="integer">
        The page number of branchs returned.
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of branchs returned.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/branchs">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/branchs', {
      params: {
        filter: {
          page:1,
          pageSize: 10
        }
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "branchs": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "name": "Super Admin",
          "permissions": ["create-user", "read-user-branch"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
        {
          "_id": "637d83d15d2be122007524bf",
          "name": "Manager",
          "permissions": ["create-user", "read-user-branch"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create a branch {{ tag: 'POST', label: '/v1/branchs' }}

<Row>
  <Col>

    This endpoint allows you to add a new branch.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the branch.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/branchs">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/branchs', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve a branch {{ tag: 'GET', label: '/v1/branchs/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve a branch by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/branchs/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/branchs/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "637d83d15d2be122007524bf",
      "name": "Manager",
      "permissions": ["create-user", "read-user-branch"],
      "createdBy": "637d83d15d2be122007524bf",
      "createdAt": "2022-01-01T00:00:00.000Z",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update a branch {{ tag: 'PATCH', label: '/v1/branchs/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a branch.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the branch.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/branchs/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/branchs/637d83d15d2be122007524bf', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete a branch {{ tag: 'DELETE', label: '/v1/branchs/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete branchs from your branch list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/branchs/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/branchs/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---
