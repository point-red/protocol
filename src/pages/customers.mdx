export const description =
  'On this page, we’ll dive into the different customer endpoints you can use to manage customers programmatically.'

# Customer

Master Customer

## The customer model

The customer model contains all the information about your customers.

### Properties

<Properties>
  <Property name="_id" type="string">
    Unique identifier for the customer.
  </Property>
  <Property name="name" type="string">
    The name for the customer.
  </Property>
  <Property name="permissions" type="array<string>">
    The array of allowed permission name for the customer.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who create customer.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who update customer.
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the customer was created.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the customer was updated.
  </Property>
</Properties>

---

## List all customers {{ tag: 'GET', label: '/v1/customers' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your customers. By default, a maximum of ten customers are shown per page.

    ### Optional attributes

    <Properties>
      <Property name="page" type="integer">
        The page number of customers returned.
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of customers returned.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/customers">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/customers', {
      params: {
        filter: {
          page:1,
          pageSize: 10
        }
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "customers": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "name": "Super Admin",
          "permissions": ["create-user", "read-user-customer"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
        {
          "_id": "637d83d15d2be122007524bf",
          "name": "Manager",
          "permissions": ["create-user", "read-user-customer"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create a customer {{ tag: 'POST', label: '/v1/customers' }}

<Row>
  <Col>

    This endpoint allows you to add a new customer.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the customer.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/customers">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/customers', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve a customer {{ tag: 'GET', label: '/v1/customers/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve a customer by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/customers/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/customers/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "637d83d15d2be122007524bf",
      "name": "Manager",
      "permissions": ["create-user", "read-user-customer"],
      "createdBy": "637d83d15d2be122007524bf",
      "createdAt": "2022-01-01T00:00:00.000Z",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update a customer {{ tag: 'PATCH', label: '/v1/customers/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a customer.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the customer.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/customers/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/customers/637d83d15d2be122007524bf', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete a customer {{ tag: 'DELETE', label: '/v1/customers/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete customers from your customer list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/customers/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/customers/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---
