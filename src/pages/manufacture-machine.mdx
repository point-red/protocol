export const description =
  'On this page, we’ll dive into the different machine endpoints you can use to manage machines programmatically.'

# Manufacture - Machine

Master Manufacture - Machine

## The machine model

The machine model contains all the information about your machines.

### Properties

<Properties>
  <Property name="_id" type="string">
    Unique identifier for the machine.
  </Property>
  <Property name="name" type="string">
    The name for the machine.
  </Property>
  <Property name="permissions" type="array<string>">
    The array of allowed permission name for the machine.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who create machine.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who update machine.
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the machine was created.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the machine was updated.
  </Property>
</Properties>

---

## List all machines {{ tag: 'GET', label: '/v1/machines' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your machines. By default, a maximum of ten machines are shown per page.

    ### Optional attributes

    <Properties>
      <Property name="page" type="integer">
        The page number of machines returned.
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of machines returned.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/machines">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/machines', {
      params: {
        filter: {
          page:1,
          pageSize: 10
        }
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "machines": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "name": "Super Admin",
          "permissions": ["create-user", "read-user-machine"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
        {
          "_id": "637d83d15d2be122007524bf",
          "name": "Manager",
          "permissions": ["create-user", "read-user-machine"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create a machine {{ tag: 'POST', label: '/v1/machines' }}

<Row>
  <Col>

    This endpoint allows you to add a new machine.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the machine.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/machines">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/machines', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve a machine {{ tag: 'GET', label: '/v1/machines/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve a machine by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/machines/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/machines/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "637d83d15d2be122007524bf",
      "name": "Manager",
      "permissions": ["create-user", "read-user-machine"],
      "createdBy": "637d83d15d2be122007524bf",
      "createdAt": "2022-01-01T00:00:00.000Z",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update a machine {{ tag: 'PATCH', label: '/v1/machines/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a machine.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the machine.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/machines/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/machines/637d83d15d2be122007524bf', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete a machine {{ tag: 'DELETE', label: '/v1/machines/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete machines from your machine list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/machines/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/machines/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---
