export const description =
  'On this page, we’ll dive into the different allocation endpoints you can use to manage allocations programmatically.'

# Allocaton

Master Allocaton

## The allocation model

The allocation model contains all the information about your allocations.

### Properties

<Properties>
  <Property name="_id" type="string">
    Unique identifier for the allocation.
  </Property>
  <Property name="name" type="string">
    The name for the allocation.
  </Property>
  <Property name="permissions" type="array<string>">
    The array of allowed permission name for the allocation.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who create allocation.
  </Property>
  <Property name="createdBy" type="string">
    User identifier who update allocation.
  </Property>
  <Property name="createdAt" type="timestamp">
    Timestamp of when the allocation was created.
  </Property>
  <Property name="updatedAt" type="timestamp">
    Timestamp of when the allocation was updated.
  </Property>
</Properties>

---

## List all allocations {{ tag: 'GET', label: '/v1/allocations' }}

<Row>
  <Col>

    This endpoint allows you to retrieve a paginated list of all your allocations. By default, a maximum of ten allocations are shown per page.

    ### Optional attributes

    <Properties>
      <Property name="page" type="integer">
        The page number of allocations returned.
      </Property>
      <Property name="pageSize" type="integer">
        The page size number of allocations returned.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/allocations">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/allocations', {
      params: {
        filter: {
          page:1,
          pageSize: 10
        }
      }
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "allocations": [
        {
          "_id": "637b569149e0c02e1036c35a",
          "name": "Super Admin",
          "permissions": ["create-user", "read-user-allocation"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
        {
          "_id": "637d83d15d2be122007524bf",
          "name": "Manager",
          "permissions": ["create-user", "read-user-allocation"],
          "createdBy": "637d83d15d2be122007524bf",
          "createdAt": "2022-01-01T00:00:00.000Z",
        },
      ],
      "pagination": {
        "page": 1,
        "pageSize": 1,
        "pageCount": 1,
        "totalDocument": 2
      }
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Create a allocation {{ tag: 'POST', label: '/v1/allocations' }}

<Row>
  <Col>

    This endpoint allows you to add a new allocation.

    ### Required attributes

    <Properties>
      <Property name="name" type="string">
        The name for the allocation.
      </Property>
    </Properties>
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="POST" label="/v1/allocations">
    ```typescript {{ title: 'axios' }}
    const response = axios.post('/v1/allocations', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '201' }}
    {
      "id": "637d83d15d2be122007524bf"
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Retrieve a allocation {{ tag: 'GET', label: '/v1/allocations/:id' }}

<Row>
  <Col>
    This endpoint allows you to retrieve a allocation by providing their id.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="GET" label="/v1/allocations/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.get('/v1/allocations/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '200' }}
    {
      "_id": "637d83d15d2be122007524bf",
      "name": "Manager",
      "permissions": ["create-user", "read-user-allocation"],
      "createdBy": "637d83d15d2be122007524bf",
      "createdAt": "2022-01-01T00:00:00.000Z",
    }
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Update a allocation {{ tag: 'PATCH', label: '/v1/allocations/:id' }}

<Row>
  <Col>

    This endpoint allows you to perform an update on a allocation.

    ### Optional attributes

    <Properties>
      <Property name="name" type="string">
        The name for the allocation.
      </Property>
    </Properties>

  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="PATCH" label="/v1/allocations/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.patch('/v1/allocations/637d83d15d2be122007524bf', {
      name: "Administrator"
    })
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---

## Delete a allocation {{ tag: 'DELETE', label: '/v1/allocations/:id' }}

<Row>
  <Col>
    This endpoint allows you to delete allocations from your allocation list.
  </Col>
  <Col sticky>
    <CodeGroup title="Request" tag="DELETE" label="/v1/allocations/637d83d15d2be122007524bf">
    ```typescript {{ title: 'axios' }}
    const response = axios.delete('/v1/allocations/637d83d15d2be122007524bf')
    ```
    </CodeGroup>
    <CodeGroup title="Response">
    ```json {{ title: '204' }}
    // empty response
    ```
    </CodeGroup>
  </Col>
</Row>

---
